transactions are all or nothing
	ACID properties: atomic, consistent isolated, d

STEPS
	begin
	execute a set of data manipualtions and or queires
	if no errors occur then commit the transactiona and end
	if errors, roll back

Most SQL languages have a default of autocommit
	how to changes this in SQL
	//must, must have
	connection.setAutoCommit(false);

set isolation level
set session transaction isolation level serializable; //you become blocked 

commit;
rollback;

use java7

check javadoc for connection